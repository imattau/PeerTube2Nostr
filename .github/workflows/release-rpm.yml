name: Build and Release RPM

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build-rpm:
    runs-on: ubuntu-latest
    container: fedora:latest
    steps:
      - name: Install build dependencies
        run: |
          dnf install -y \
            rpm-build \
            rpmdevtools \
            python3-devel \
            python3-setuptools \
            python3-pip \
            python3-wheel \
            python3-virtualenv \
            desktop-file-utils \
            git

      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Get version from tag
        id: version
        run: |
          if [ "${{ github.ref_type }}" = "tag" ]; then
            echo "version=${{ github.ref_name }}" | sed 's/^v//' >> $GITHUB_OUTPUT
          else
            echo "version=$(grep '^Version:' peertube2nostr.spec | awk '{print $2}')" >> $GITHUB_OUTPUT
          fi

      - name: Set up RPM build directories
        run: |
          mkdir -p ~/rpmbuild/{BUILD,RPMS,SOURCES,SPECS,SRPMS}

      - name: Create source tarball
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          tar czf ~/rpmbuild/SOURCES/peertube2nostr-${VERSION}.tar.gz \
            --transform "s,^,peertube2nostr-${VERSION}/," \
            .

      - name: Build RPM package
        run: |
          rpmbuild -ba peertube2nostr.spec

      - name: Find built RPM files
        id: find_rpms
        run: |
          find ~/rpmbuild/RPMS -name "*.rpm" -type f > rpms.txt
          cat rpms.txt

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          name: "PeerTube2Nostr ${{ steps.version.outputs.version }}"
          generate_release_notes: true
          files: |
            ~/rpmbuild/RPMS/noarch/*.rpm
